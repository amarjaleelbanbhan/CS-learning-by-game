/**
 * @arc/engine-animation
 *
 * Timeline / tween model: turns a Trace into time-indexed keyframes. Adapters drive Framer Motion / GSAP.
 *
 * Phase 2 scaffold — public API to be implemented in its dedicated phase.
 */
export const PACKAGE_NAME = '@arc/engine-animation' as const;
