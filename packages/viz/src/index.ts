/**
 * @arc/viz
 *
 * React renderers (VizSurface, AutomatonGraph, TimelineScrubber). React Flow / SVG / Canvas land in Phase 5.
 *
 * Phase 2 scaffold — public API to be implemented in its dedicated phase.
 */
export const PACKAGE_NAME = '@arc/viz' as const;
