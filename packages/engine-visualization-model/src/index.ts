/**
 * @arc/engine-visualization-model
 *
 * Graph auto-layout + node/edge geometry + camera model. Produces render-ready coordinates; no DOM.
 *
 * Phase 2 scaffold — public API to be implemented in its dedicated phase.
 */
export const PACKAGE_NAME = '@arc/engine-visualization-model' as const;
